import 'package:flutter/material.dart';

class CompanionHeatmap extends StatelessWidget {
  const CompanionHeatmap({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
